# ai_agent

Package root. Contains no public exports — import directly from sub-packages.

## Sub-packages

| Package | Description |
|---------|-------------|
| `adapters/` | Concrete implementations of the provider interfaces defined in `core/` |
| `cli/` | Command-line entry point |
| `core/` | All agent intelligence and business logic |

## Files

- `__init__.py` — package marker, no exports.
